const {
    validatePath,
    existsPath,
    readFileMd,
  } = require("./src/index.js");
  
  const mdLinks = (path, options) => {
    // console.log(validatePath);
  };
  
  module.exports = {
    mdLinks,
  };
  